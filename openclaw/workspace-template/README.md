# Workspace Template

This directory documents the runtime layout created under `~/.toprank/openclaw`.

The files here are templates and examples only. Do not store live operator data in the git repo.
